apiVersion: v1
kind: Service
metadata:
  name: garage-web-lb
  namespace: {{ .Release.Namespace }}
  annotations:
    lbipam.cilium.io/ips: {{ .Values.webLoadBalancerIP | quote }}
spec:
  type: LoadBalancer
  selector:
    app: caddy
  ports:
    - name: http
      port: 80
      targetPort: 80
