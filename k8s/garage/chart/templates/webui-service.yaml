{{- if .Values.webui.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: garage-webui
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: garage-webui
  ports:
    - name: http
      port: 3909
      targetPort: 3909
{{- end }}
