openbao:
  adminPassword:
    key: k8s/argocd/sandbox/admin

server:
  ingress:
    hostname: argocd.sandbox.butaco.net
  route:
    gatewayName: shared-gateway-envoy
    gatewayNamespace: gateway-system
    sectionName: http
