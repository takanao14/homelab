configs:
  cm:
    helm.valuesFileSchemes: secrets+age-import, secrets+gpg-import, secrets+age-import-kubernetes, secrets+gpg-import-kubernetes, secrets, secrets+literal
  cmp:
    create: true
    plugins:
      helm-secrets:
        allowConcurrency: true
        discover:
          find:
            glob: "**/Chart.yaml"
        init:
          command: [sh, -c]
          args:
            - |
              if grep -q "^dependencies:" Chart.yaml 2>/dev/null; then
                helm dependency update .
              fi
        generate:
          command: [sh, -c]
          args:
            - helm secrets template "$ARGOCD_APP_NAME" . --namespace "$ARGOCD_APP_NAMESPACE" $(echo "$ARGOCD_ENV_HELM_VALUES" | tr ',' '\n' | sed 's/^/-f /' | tr '\n' ' ')
        lockRepo: false

server:
  extraArgs:
    - --insecure
  ingress:
    enabled: false

repoServer:
  volumes:
    - name: plugins
      emptyDir: {}
    - name: helm-secrets-private-keys
      secret:
        secretName: helm-secrets-private-keys
    - name: cmp-tmp
      emptyDir: {}
    - name: argocd-cmp-cm
      configMap:
        name: argocd-cmp-cm
  extraContainers:
    - name: helm-secrets-cmp
      image: ghcr.io/takanao14/argocd-helm-secrets-cmp:latest
      securityContext:
        runAsNonRoot: true
        runAsUser: 999
      env:
        - name: HELM_SECRETS_BACKEND
          value: sops
        - name: SOPS_AGE_KEY_FILE
          value: /helm-secrets-private-keys/key.txt
        - name: HELM_CACHE_HOME
          value: /tmp/helm-cache
      volumeMounts:
        - mountPath: /home/argocd/cmp-server/plugins
          name: plugins
        - mountPath: /tmp
          name: cmp-tmp
        - mountPath: /helm-secrets-private-keys/
          name: helm-secrets-private-keys
        - mountPath: /home/argocd/cmp-server/config/plugin.yaml
          subPath: helm-secrets.yaml
          name: argocd-cmp-cm
