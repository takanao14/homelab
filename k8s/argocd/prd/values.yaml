global:
  dnsConfig:
    options:
      - name: ndots
        value: "2"

server:
  ingress:
    hostname: argocd-ui.prd.butaco.net
  service:
    annotations:
      external-dns.alpha.kubernetes.io/hostname: argocd.prd.butaco.net
    type: LoadBalancer
