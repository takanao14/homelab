openbao:
  adminPassword:
    key: k8s/argocd/prd/admin

server:
  ingress:
    hostname: argocd-ui.prd.butaco.net
  route:
    gatewayName: shared-gateway-envoy
    gatewayNamespace: gateway-system
    sectionName: https
  service:
    annotations:
      external-dns.kubernetes.io/hostname: argocd.prd.butaco.net
    type: LoadBalancer
