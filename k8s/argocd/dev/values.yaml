server:
  ingress:
    hostname: argocd-ui.dev.butaco.net
  route:
    gatewayName: shared-gateway-envoy
    gatewayNamespace: gateway-system
    sectionName: https
  service:
    annotations:
      external-dns.alpha.kubernetes.io/hostname: argocd.dev.butaco.net
    type: LoadBalancer
