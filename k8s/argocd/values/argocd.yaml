server:
  extraArgs:
    - --insecure
  ingress:
    enabled: true
    ingressClassName: cilium
    annotations:
      external-dns.alpha.kubernetes.io/hostname: argocd.dev.butaco.net
    hostname: argocd.dev.butaco.net
