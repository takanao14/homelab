apiVersion: v1
kind: Service
metadata:
  name: meshcentral
  namespace: {{ .Release.Namespace }}
  labels:
    app: meshcentral
spec:
  type: ClusterIP
  selector:
    app: meshcentral
  ports:
    - protocol: TCP
      name: http
      port: 443
      targetPort: 443
