environments:
  dev: {}
  prd: {}
---
releases:
  - name: homepage
    namespace: homepage
    chart: ./chart
    createNamespace: true
    values:
      - values-common.yaml.gotmpl
      - values-{{ .Environment.Name }}.yaml.gotmpl
