apiVersion: v1
kind: Service
metadata:
  name: homepage
  namespace: {{ .Values.namespace }}
  labels:
    app: homepage
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
  selector:
    app: homepage
