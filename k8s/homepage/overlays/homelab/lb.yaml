apiVersion: v1
kind: Service
metadata:
  name: homepage-lb
  labels:
    app: homepage
  annotations:
    external-dns.alpha.kubernetes.io/hostname: homepage.k8s.homelab.internal
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
  selector:
    app: homepage
