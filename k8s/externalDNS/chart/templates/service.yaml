apiVersion: v1
kind: Service
metadata:
  name: external-dns
  labels:
    app: external-dns
spec:
  selector:
    app: external-dns
  ports:
    - name: http-metrics
      port: 7979
      targetPort: http-metrics
