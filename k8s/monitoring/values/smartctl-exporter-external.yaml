port: 9633
metricsPath: /metrics

# TrueNAS owns the passed-through disks, so its local exporter is authoritative.
# Names label IP-based scrapes without a DNS dependency.
targets:
  - ip: "192.168.20.10"
    name: "truenas"
