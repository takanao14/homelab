apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: external-amd-gpu-monitor
  namespace: {{ .Release.Namespace }}
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: external-amd-gpu
  endpoints:
    - port: metrics
      interval: 30s
      path: {{ .Values.metricsPath }}
