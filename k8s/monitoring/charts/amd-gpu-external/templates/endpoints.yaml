apiVersion: v1
kind: Endpoints
metadata:
  name: external-amd-gpu
  namespace: {{ .Release.Namespace }}
  labels:
    app: external-amd-gpu
    release: prometheus
subsets:
  - addresses:
      - ip: {{ .Values.targetIP | quote }}
    ports:
      - name: metrics
        port: {{ .Values.port }}
        protocol: TCP
