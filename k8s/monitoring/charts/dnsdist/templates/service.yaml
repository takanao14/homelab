apiVersion: v1
kind: Service
metadata:
  name: dnsdist-metrics
  namespace: {{ .Release.Namespace }}
  labels:
    app: dnsdist
    release: prometheus
spec:
  clusterIP: None
  ports:
    - name: metrics
      protocol: TCP
      port: {{ .Values.port }}
      targetPort: {{ .Values.port }}
