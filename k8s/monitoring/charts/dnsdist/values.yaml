port: 8083
metricsPath: /metrics

# Shared DNS frontend inventory.
dnsFrontends: []

alerts:
  enabled: false
  # Redundancy permits two health-check rounds.
  backendDownFor: 2m
  # No resolver remains to absorb failure.
  noResolverFor: 1m
