apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: pdns-auth-monitor
  namespace: {{ .Release.Namespace }}
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: pdns-auth
  endpoints:
    - port: metrics
      interval: 30s
      path: {{ .Values.metricsPath }}
