apiVersion: v1
kind: Service
metadata:
  name: pdns-auth-metrics
  namespace: {{ .Release.Namespace }}
  labels:
    app: pdns-auth
    release: prometheus
spec:
  clusterIP: None
  ports:
    - name: metrics
      protocol: TCP
      port: {{ .Values.port }}
      targetPort: {{ .Values.port }}
