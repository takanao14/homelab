blackboxExporterURL: ""
scrapeInterval: 30s

# Shared Proxmox inventory.
proxmoxNodes: []

# Shared frontend inventory for both DNS probe modules.
dnsFrontends: []

icmp:
  targets: []

# Service ports probed with the tcp_connect module.
tcp:
  targets: []

dnsProbeAlerts:
  enabled: false
  # Client-visible failure across two independently probed frontends.
  failedFor: 2m

tcpProbeAlerts:
  enabled: false
  # A closed port is unambiguous; wait only long enough to skip a restart.
  downFor: 10m

icmpProbeAlerts:
  enabled: false
  # Ignore minute-scale blips; sustained outages exceeded this threshold.
  downFor: 10m
  # AMT is a recovery path; tolerate routine brief drops.
  amtDownFor: 1h
