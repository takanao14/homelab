{{- if .Values.necIx.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: snmp-exporter-nec-ix
  namespace: {{ .Release.Namespace }}
data:
  nec-ix.yml: |-
{{ .Files.Get "files/nec-ix.yml" | indent 4 }}
{{- end }}
