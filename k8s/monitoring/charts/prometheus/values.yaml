prometheus:
  hostname: ""

gateway:
  name: shared-gateway-envoy
  namespace: gateway-system
  sectionName: https

alertmanagerConfig:
  enabled: true

externalSecretAlertmanager:
  enabled: true

prometheusRules:
  argocd:
    enabled: true
  certManager:
    enabled: true
  hardware:
    enabled: true
