apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-prd-butaco-net
  namespace: cert-manager
spec:
  secretName: wildcard-prd-butaco-net-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - "*.prd.butaco.net"
