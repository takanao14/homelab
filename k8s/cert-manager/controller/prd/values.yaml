# No prd-specific overrides for the cert-manager controller chart.
# cluster=prd is injected by the default Prometheus scrapeClass cluster-prd
# (ADR-0016, prometheusSpec.scrapeClasses in k8s/monitoring/values/prometheus.yaml),
# so no per-ServiceMonitor relabeling is needed here.
