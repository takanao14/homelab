apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-prd
  namespace: cert-manager
spec:
  secretName: wildcard-prd-tls
  dnsNames:
    - "*.prd.butaco.net"
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
