apiVersion: v1
kind: ConfigMap
metadata:
  name: lemonade-server-config
  namespace: {{ .Release.Namespace }}
data:
  config.json: |
{{ .Values.config | toPrettyJson | nindent 4 }}
  recipe_options.json: |
{{ .Values.recipeOptions | toPrettyJson | nindent 4 }}
