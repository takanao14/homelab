apiVersion: v1
kind: ConfigMap
metadata:
  name: searxng-settings
  namespace: {{ .Release.Namespace }}
data:
  settings.yml: |
    use_default_settings: true
    server:
      bind_address: "0.0.0.0"
      port: 8080
      limiter: false
    search:
      formats:
        - json
