---
podman_packages:
  - podman
  - uidmap
  - slirp4netns
  - fuse-overlayfs

# Root system Quadlet unit directory (systemd auto-generates *.service units
# from *.container/*.pod/*.volume files here on `systemctl daemon-reload`).
podman_quadlet_dir: /etc/containers/systemd
