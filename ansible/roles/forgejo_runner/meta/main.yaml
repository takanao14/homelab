---
# Ensure Docker runs before adding the runner user to its group.
dependencies:
  - role: docker
    vars:
      docker_daemon_config:
        builder:
          gc:
            enabled: true
            defaultKeepStorage: "{{ forgejo_runner_build_cache_keep_storage }}"
