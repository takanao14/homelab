---
- name: Create forgejo group
  become: true
  ansible.builtin.group:
    name: "{{ forgejo_group }}"
    system: true
    state: present

- name: Create forgejo user
  become: true
  ansible.builtin.user:
    name: "{{ forgejo_user }}"
    group: "{{ forgejo_group }}"
    home: "{{ forgejo_home }}"
    shell: /bin/bash
    system: true
    state: present

- name: Create required directories
  become: true
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    owner: "{{ forgejo_user }}"
    group: "{{ forgejo_group }}"
    mode: '0750'
  loop:
    - "{{ forgejo_home }}"
    - "{{ forgejo_home }}/data"
    - "{{ forgejo_home }}/repos"
    - "{{ forgejo_home }}/log"
    - "{{ forgejo_config_dir }}"

- name: Install git
  become: true
  ansible.builtin.apt:
    name: git
    state: present
    update_cache: true

- name: Download forgejo binary
  become: true
  ansible.builtin.get_url:
    url: "https://codeberg.org/forgejo/forgejo/releases/download/v{{ forgejo_version }}/forgejo-{{ forgejo_version }}-linux-amd64"
    dest: "{{ forgejo_binary }}"
    owner: root
    group: root
    mode: '0755'
  notify: Restart forgejo

- name: Ensure homelab version marker directory exists
  become: true
  ansible.builtin.file:
    path: /usr/local/share/homelab-versions
    state: directory
    owner: root
    group: root
    mode: '0755'

- name: Record installed forgejo version
  become: true
  ansible.builtin.copy:
    dest: /usr/local/share/homelab-versions/forgejo.version
    content: "{{ forgejo_version }}\n"
    owner: root
    group: root
    mode: '0644'

- name: Deploy forgejo app.ini
  become: true
  ansible.builtin.template:
    src: app.ini.j2
    dest: "{{ forgejo_config_dir }}/app.ini"
    owner: "{{ forgejo_user }}"
    group: "{{ forgejo_group }}"
    mode: '0640'
  notify: Restart forgejo

- name: Deploy forgejo systemd unit
  become: true
  ansible.builtin.template:
    src: forgejo.service.j2
    dest: /etc/systemd/system/forgejo.service
    owner: root
    group: root
    mode: '0644'
  notify:
    - Reload systemd
    - Restart forgejo

- name: Ensure forgejo is started and enabled
  become: true
  ansible.builtin.service:
    name: forgejo
    state: started
    enabled: true
