---
# Bundle Vector and journald for LXC. Declare after the service role to preserve
# service -> vector -> journald ordering; dependency tags remain usable.
dependencies:
  - role: vector
    tags: [logging, vector]
  - role: journald
    tags: [logging, journald]
