---
# Persistent/live sysctls; overrides replace the whole mapping rather than merge.
sysctl_conf_file: /etc/sysctl.d/99-ansible.conf

sysctl_settings:
  # Avoid pre-emptive VM swapping while retaining an OOM safety valve; do not use 0.
  vm.swappiness: 10
