---
# Time sync for physical hosts and VMs; LXC inherits the host clock.
- name: Configure chrony time synchronization
  hosts: all:!lxc:!power_only
  any_errors_fatal: false
  roles:
    - role: chrony
      tags: chrony
