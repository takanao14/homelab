---
# Wait until every node is registered and Ready; API startup failures are retried.

- name: Wait for every node to report Ready
  ansible.builtin.command:
    argv:
      - kubectl
      - "--kubeconfig={{ k8s_kubeconfig | expanduser }}"
      - get
      - nodes
      - -o
      - jsonpath={range .items[*]}{.status.conditions[?(@.type=="Ready")].status}{"\n"}{end}
  register: startup_node_ready
  until:
    - startup_node_ready.stdout_lines | length > 0
    - startup_node_ready.stdout_lines | reject('equalto', 'True') | list | length == 0
  # Four-hypervisor rehearsal needed 39 attempts; allow margin for all six.
  retries: 90
  delay: 10
  delegate_to: localhost
  become: false
  changed_when: false
