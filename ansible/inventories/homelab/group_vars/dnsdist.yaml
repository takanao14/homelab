---
# health_checks is the YAML config's nested form of the old checkName/checkType
# pair. Each resolver is probed with a name it actually answers for.
dnsdist_default_servers:
  - name: local-forwarder
    address: "192.168.10.1:53"
    health_checks:
      qname: "google.com."
      qtype: A
  - name: google-dns
    address: "8.8.8.8:53"
    health_checks:
      qname: "google.com."
      qtype: A
  - name: cloudflare-dns
    address: "1.1.1.1:53"
    health_checks:
      qname: "cloudflare.com."
      qtype: A

vector_config:
  sources:
    journald:
      type: journald
      include_units:
        - dnsdist
        - dnscollector
        - ssh
  transforms:
    parse_journald:
      type: remap
      inputs:
        - journald
      source: |
        .host = get_hostname!()
        .unit = string(._SYSTEMD_UNIT) ?? string(.SYSLOG_IDENTIFIER) ?? "unknown"
  sinks:
    loki:
      type: loki
      inputs:
        - parse_journald
      endpoint: "{{ loki_endpoint }}"
      encoding:
        codec: json
      healthcheck:
        enabled: false
      dangerously_allow_unconfined_template_resolution: true
      labels:
        host: !unsafe "{{ host }}"
        unit: !unsafe "{{ unit }}"
