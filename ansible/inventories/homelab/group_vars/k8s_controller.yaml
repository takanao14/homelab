---
k0s_service: k0scontroller

# Require all namespaces to be classified before shutdown.
k8s_shutdown_allow_unclassified: false

# Persist pre-shutdown replicas outside the repo for startup restoration.
k8s_shutdown_state_dir: "~/.local/state/homelab/shutdown"
