source_up

# This stack manages a VM on node5 while the parent env dir is bound to
# node1: re-source the node5 secrets after the parent chain so the Proxmox
# provider credentials point at node5.
set -a
source <(sops --decrypt ../../../.env/secrets.node5.enc.env)
set +a
