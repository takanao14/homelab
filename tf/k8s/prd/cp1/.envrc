source_up

# This stack manages a VM on node4 while the parent env dir is bound to
# node1: re-source the node4 secrets after the parent chain so the Proxmox
# provider credentials point at node4.
set -a
source <(sops --decrypt ../../../.env/secrets.node4.enc.env)
set +a
