source_up

# This stack manages a VM on pve while the parent env dir is bound to
# node1: re-source the pve secrets after the parent chain so the Proxmox
# provider credentials point at pve.
set -a
source <(sops --decrypt ../../../.env/secrets.dev.enc.env)
set +a
